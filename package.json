{
  "name": "@elgon/api",
  "version": "0.1.0",
  "description": "Market data API \u2014 stock & crypto quotes, options, instrument search",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ --ext .ts",
    "format": "prettier --write ."
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "yahoo-finance2": "^2.13.0",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0",
    "eslint": "^9.12.0",
    "prettier": "^3.4.0",
    "@types/node": "^22.0.0",
    "@types/express": "^5.0.0"
  }
}

// redirect to correct success URL
